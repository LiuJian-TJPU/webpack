import './index.css';
import './index.less';

export const b = (a, b) => {
  document.querySelector('.b').innerHTML = a + b
}

export const c = (hello, world) => {
  document.querySelector('.c').innerHTML = a + b
}